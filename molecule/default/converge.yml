---
- name: Converge
  hosts: all
  environment:
    GITHUB_ACTIONS: "maybe"
    ANSIBLE_HASH_BEHAVIOUR: merge
  tasks:
    - name: "Include ansible-gitlab-podman-runner"
      ansible.builtin.include_role:
        name: "ansible-gitlab-podman-runner"
